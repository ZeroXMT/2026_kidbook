Check the concepts file.json for compliance. There is a reference file in the TUTORIAL directory. 
Also check the number of articles - there should be 3*command_size, where command_size is the size of the participating team. Their names are written in the concepts file.json in the "members" field as an array of strings.
